class NoInternetError{
  String msg;
  NoInternetError({this.msg='No internet'});
}
class ServerError{
  String msg;
  ServerError({this.msg='Server Error'});
}

class NoData{
  String msg;
  NoData({this.msg='No data found'});
}