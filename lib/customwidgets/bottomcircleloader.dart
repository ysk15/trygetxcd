import 'package:flutter/material.dart';
class BottomCircularLoader extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(height:32,width: 32,child: CircularProgressIndicator());
  }
}
